# Debug configuration - verbose output, no compression
# Usage: python3 run_simulation.py --config configs/debug.yaml

simulation:
  years: 20
  growth_phase: 5    # 32 cells
  sites: 500

output:
  compress: false    # Easy to inspect
  directory: "data/debug"

performance:
  track_gene_jsd: true
  calculate_cell_jsds: true

seed: 999