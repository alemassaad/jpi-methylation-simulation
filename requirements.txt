# Core dependencies (none required for basic simulation)
# Python 3.7+ standard library is sufficient for simulation

# Config file support
pyyaml>=6.0

# Plotting dependencies
plotly>=5.0.0,<6.0.0
kaleido==0.2.1