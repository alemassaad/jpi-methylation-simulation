--- run_pipeline.py.orig
+++ run_pipeline.py
@@ -45,6 +45,11 @@ def run_pipeline(args):
     
     start_time = time.time()
     
+    # Set global random seeds for reproducibility
+    import random
+    random.seed(args.seed)
+    print(f"Global random seed set to {args.seed}")
+    
     # ========================================================================
     # SETUP
     # ========================================================================